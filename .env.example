# Copy this file to `.env` and paste in the read-only access key you were given.
# `.env` is gitignored — never commit your real keys.
#
# The whole cohort SHARES this one read+write key. You can save your own work to
# the bucket, but you can also overwrite/delete shared data or a teammate's files
# — so always write under your personal folder (set WASABI_USER below) and never
# touch the shared source datasets.

WASABI_ACCESS_KEY=your-access-key-here
WASABI_SECRET_KEY=your-secret-key-here
WASABI_REGION=eu-west-1
WASABI_BUCKET=nffcfirstteamstudents

# Your name/initials — your work is saved under students/<WASABI_USER>/
WASABI_USER=your-name
